# This file can be used to define a base class or common utilities
# for CRUD operations, especially if using an ORM.

# For our direct SQL approach, we don't need a base class,
# so this file will remain simple. We will place all logic
# in the specific files (user.py, project.py, etc.).
pass
